// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: oblivio/v1/subscriptions.proto

package obliviov1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/sxwebdev/oblivio/internal/api/gen/go/oblivio/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SubscriptionsServiceName is the fully-qualified name of the SubscriptionsService service.
	SubscriptionsServiceName = "oblivio.v1.SubscriptionsService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SubscriptionsServiceSubscribeProcedure is the fully-qualified name of the SubscriptionsService's
	// Subscribe RPC.
	SubscriptionsServiceSubscribeProcedure = "/oblivio.v1.SubscriptionsService/Subscribe"
)

// SubscriptionsServiceClient is a client for the oblivio.v1.SubscriptionsService service.
type SubscriptionsServiceClient interface {
	// Subscribe opens a server stream of per-user notifications. The
	// procedure requires authentication; the server scopes notifications to
	// the caller's user_id. Closing the client cancels the stream.
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.SubscribeResponse], error)
}

// NewSubscriptionsServiceClient constructs a client for the oblivio.v1.SubscriptionsService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSubscriptionsServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SubscriptionsServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	subscriptionsServiceMethods := v1.File_oblivio_v1_subscriptions_proto.Services().ByName("SubscriptionsService").Methods()
	return &subscriptionsServiceClient{
		subscribe: connect.NewClient[v1.SubscribeRequest, v1.SubscribeResponse](
			httpClient,
			baseURL+SubscriptionsServiceSubscribeProcedure,
			connect.WithSchema(subscriptionsServiceMethods.ByName("Subscribe")),
			connect.WithClientOptions(opts...),
		),
	}
}

// subscriptionsServiceClient implements SubscriptionsServiceClient.
type subscriptionsServiceClient struct {
	subscribe *connect.Client[v1.SubscribeRequest, v1.SubscribeResponse]
}

// Subscribe calls oblivio.v1.SubscriptionsService.Subscribe.
func (c *subscriptionsServiceClient) Subscribe(ctx context.Context, req *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.SubscribeResponse], error) {
	return c.subscribe.CallServerStream(ctx, req)
}

// SubscriptionsServiceHandler is an implementation of the oblivio.v1.SubscriptionsService service.
type SubscriptionsServiceHandler interface {
	// Subscribe opens a server stream of per-user notifications. The
	// procedure requires authentication; the server scopes notifications to
	// the caller's user_id. Closing the client cancels the stream.
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.SubscribeResponse]) error
}

// NewSubscriptionsServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSubscriptionsServiceHandler(svc SubscriptionsServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	subscriptionsServiceMethods := v1.File_oblivio_v1_subscriptions_proto.Services().ByName("SubscriptionsService").Methods()
	subscriptionsServiceSubscribeHandler := connect.NewServerStreamHandler(
		SubscriptionsServiceSubscribeProcedure,
		svc.Subscribe,
		connect.WithSchema(subscriptionsServiceMethods.ByName("Subscribe")),
		connect.WithHandlerOptions(opts...),
	)
	return "/oblivio.v1.SubscriptionsService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SubscriptionsServiceSubscribeProcedure:
			subscriptionsServiceSubscribeHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSubscriptionsServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSubscriptionsServiceHandler struct{}

func (UnimplementedSubscriptionsServiceHandler) Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.SubscribeResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("oblivio.v1.SubscriptionsService.Subscribe is not implemented"))
}
